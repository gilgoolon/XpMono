name: ReSharper CodeCleanup

on: [ pull_request ]

jobs:
    find-solutions:
        runs-on: ubuntu-latest
        outputs:
            sln_files: ${{ steps.find_slns.outputs.sln_files }}
        steps:
            - name: Checkout Code
              uses: actions/checkout@v4
  
            - name: Find Solution Files
              id: find_slns
              run: echo "sln_files=$(find . -name '*.sln' | jq -R -s -c 'split("\n")[:-1]')" >> $GITHUB_ENV

    cleanup:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                solution: ${{ fromJson(needs.find-solutions.outputs.sln_files) }}

        steps:
            -   name: Checkout
                uses: actions/checkout@v3

            -   name: Cleanup Code
                id: cleanup
                uses: ArturWincenciak/ReSharper_CleanupCode@v4.14
                with:
                    solution: '${{ matrix.solution }}'
                    fail_on_reformat_needed: 'yes'
                    jb_cleanup_code_arg: '--verbosity=INFO --exclude=Exetrnal/ --settings=XpSettings.DotSettings'
